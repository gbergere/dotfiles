# MacOSX/Linux
*.swp
*.swo
.DS_Store

# Binaries
*.zip

# Terraform
.terraform/
terraform.tfstate*
.terraform.tfstate.lock*

# Hammerspoon
hs

# Claude Code — this is the global gitignore (symlinked to ~/.gitignore),
# so .claude/ is ignored in all repos to avoid committing local Claude config.
# The dotfiles repo itself tracks .claude/ via `git add -f`.
.claude/
